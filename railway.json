{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "apt-get update && apt-get install -y python3.9 python3.9-venv python3.9-distutils && python3.9 -m ensurepip && python3.9 -m pip install --upgrade pip && python3.9 -m pip install -r backend/requirements.txt && cd frontend && npm install && npm run build"
    },
    "deploy": {
      "startCommand": "uvicorn backend.main:app --host 0.0.0.0 --port $PORT",
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 10
    }
  }  