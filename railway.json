{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python -m pip install --upgrade pip && cd backend && python -m pip install -r requirements.txt && cd ../frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && python -m uvicorn app:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 